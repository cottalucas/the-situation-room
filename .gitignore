node_modules
dist
.DS_Store
*.local

# env and secrets (Firebase config arrives via env in the next pass)
.env
.env.*
!.env.example

# local key material, never commit
*.pem
*.key
secrets/

# Firebase CLI deploy cache
.firebase/
firebase-debug.log*

# local LLM traces contain private prompts and raw model responses
llm-traces/
