# CLAUDE.md

Before any task in this repo, run the loop in `docs/orchestration.md`.

Read `docs/brief.md`, `docs/architecture.md`, `docs/design-system.md`,
`docs/roadmap.md`, `docs/llm-pipeline.md`, and `docs/resolution-log.md` first.
Build to those docs. Update them and append a resolution log entry when you are
done. There are no audit reports; the running record is `docs/resolution-log.md`
and the rest of the `docs/` set.

The `docs/` set is the source of truth, not this file. This file only points
there so every tool starts in the same place.
